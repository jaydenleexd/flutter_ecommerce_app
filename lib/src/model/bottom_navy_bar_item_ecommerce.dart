import 'package:flutter/material.dart' show Icon, Color;

class BottomNavyBarItemEcommerce {
  String title;
  Icon icon;
  Color activeColor;
  Color inActiveColor;

  BottomNavyBarItemEcommerce(
    this.title,
    this.icon,
    this.activeColor,
    this.inActiveColor,
  );
}
